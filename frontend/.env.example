# Frontend environment variables (example)
# NEXT_PUBLIC_API_BASE should point to your backend API base URL in production
NEXT_PUBLIC_API_BASE=https://api.tu-dominio.com

# If deploying to Vercel, set the above in Project Settings -> Environment Variables
